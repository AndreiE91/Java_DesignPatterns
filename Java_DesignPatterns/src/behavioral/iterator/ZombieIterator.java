package behavioral.iterator;

import java.util.Iterator;

interface ZombieIterator extends Iterator<Zombie> {
    // No additional methods needed, but if they were, this is where they would be declared
}
